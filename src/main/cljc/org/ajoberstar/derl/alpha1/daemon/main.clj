(ns org.ajoberstar.derl.alpha1.daemon.main
  (:require [clojure.string :as string]
            [com.stuartsierra.component :as comp]))

(defn start [& {:keys [address port] :as opts}]
  (let [address]))

(defn -main [& args])  
